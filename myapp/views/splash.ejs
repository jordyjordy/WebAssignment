<!doctype HTML>
<html>
    <head>
        <title>Reversi</title>
        <meta name="viewport" content="maximum-scale=1.0">
        <link rel="stylesheet" type="text/css" href="./stylesheets/splash.css">
    </head>

    <body>
        <div id="left" class="side">

            <div id="howto" class="button">
                <img src="./images/white.png"/>
                <div><button onclick="showTutorial();">How To</button></div>
            </div>
            
            <h1 id="rev" class="title">Rev</h1>

            <div class="stat">
                    <p>Amount of times you've visited this page: 
                        <%= plays %>
                            <%if (plays != 1) { %>
                                times
                            <% } else {%>
                               time
                            <% } %>
                        </p>
                <p>Total amount of games played: 
			<%= gamesPlayed %>
				<%if (gamesPlayed != 1) { %>
					games
				<% } else {%>
					game
				<% } %>
		</p>
                <p>Amount of games in progress: 
			<%= gamesInProgress %>
                <%if (gamesPlayed != 1) { %>
                    games
                <% } else {%>
                  game
                <% } %>
		</p>
            </div>
        </div>

        <div id="tutorial">
            <p>Try to fill the board with as many chips of your colour as you can!</p> 
            <p>A chip must be placed so that there exists a chip of another colour between 
            it and an anchcoring of the player's colour chip.
            The chips between with another colour will be flipped. This can go horizontally,
            vertically and diagonally.
            </p>
            <p>The game is over when there are no moves left or when all chips have been captured by one colour.</p>

            <button onclick="hideTutorial();">OK</button>
        </div>

        <div id ="right" class="side">

            <div id="play" class="button">
                <img src="./images/black.png" />
                <div><a href="./game.html">Play</a></div>
            </div>

            <h1 id="ersi" class="title">ersi</h1>

            <div class="stat">
                <p>Amount of games quit before finishing:
                    <%= gamesCancelled %>
                    <%if (gamesCancelled != 1) { %>
                        games
                    <% } else {%>
                        game
                    <% } %>
		        </p>
                <p>Win ratio white/black: 
			        <%= whiteWins %> / <%= blackWins %>
		        </p>
            </div>
        </div>

        <script>
            function hideTutorial(){
                document.getElementById("tutorial").style.display = "none";
            }
            function showTutorial(){
                document.getElementById("tutorial").style.display = "block";
            }
        </script>
    </body>


</html>